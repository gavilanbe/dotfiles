# Init

source "$DOTFILES_PATH/shell/init.sh"

# Start zim
source "$ZIM_HOME/init.zsh"

#source "$DOTFILES_PATH/shell/zsh/themes/spaceship.zsh"

# Bindings

source "$DOTFILES_PATH/shell/zsh/bindings/reverse_search.zsh"
